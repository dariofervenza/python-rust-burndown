[package]
name = "burndown_rs"
version = "0.1.0"
edition = "2024"

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
chrono = { version = "0.4", features = ["serde"] }

[lib]
crate-type = ["cdylib"]
